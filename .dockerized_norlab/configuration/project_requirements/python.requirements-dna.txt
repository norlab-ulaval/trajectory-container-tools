# =================================================================================================
# DNA python requirements
#
# Notes:
# - This file is used in DNA Dockerfile.project-core-pre via pip install
# - It is executed after shell.requirements-dna.bash
#
# Doc
#  - Requirements File Format:  https://pip.pypa.io/en/stable/reference/requirements-file-format/
#  - Requirement Specifiers:  https://pip.pypa.io/en/stable/reference/requirement-specifiers/
#
# =================================================================================================


# Jupyter notebook authoring applications
# https://jupyterlab.readthedocs.io/en/latest/
jupyterlab

# Single-command clean up for Python bytecode files (i.e. __pycache__; .*pyc;*.pytest_cache;)
# to prevent cache related error in remote development setup
# https://github.com/bittner/pyclean
pyclean

# pyproject.toml dynamic version management
# https://pypi.org/project/setuptools-scm
# Require adding the following to pyproject.toml
#   >>> [build-system]
#   >>> requires = ["setuptools>=61.0", "setuptools-scm>=8"]
#   >>> [tool.setuptools_scm]
#setuptools-scm

# (CRITICAL) inprogress: NMO-802 fix: matplotlib double install conflict
# (Priority) ToDo: on task NMO-802 end >> validate it's still required ↓↓
# Note: Set DNA to build for Numpy 2 but let the pyproject.toml dependencies open so that we keep
# support for Numpy 1
#matplotlib >= 3.8.0  # These versions support NumPy 2.x
#numpy >= 2.0.0
